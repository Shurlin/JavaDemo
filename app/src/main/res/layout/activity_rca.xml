<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.list.DigitalLogic.RCA_Activity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_rca"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:title="串行进位全加法器（RCA）原理" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:layout_constraintBottom_toTopOf="@id/rcaButtonBar"
        app:layout_constraintTop_toBottomOf="@id/toolbar_rca"
        tools:src="@tools:sample/backgrounds/scenic" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:id="@+id/rcaButtonBar"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="4dp">


        <Button
            android:id="@+id/prevButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:padding="0dp"
            android:layout_margin="2dp"
            android:backgroundTint="@color/button_green"
            android:text="上一页" />


        <TextView
            android:id="@+id/pageView"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="TextView"
            android:gravity="center"/>

        <Button
            android:id="@+id/firstButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:padding="0dp"
            android:layout_margin="2dp"
            android:backgroundTint="@color/button_orange"
            android:text="返回首页" />


        <Button
            android:id="@+id/nextButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:padding="0dp"
            android:layout_margin="2dp"
            android:backgroundTint="@color/button_blue"
            android:text="下一页" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
