<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 允许访问 getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS) 下的文件 -->
    <external-files-path
        name="external_files_downloads"
        path="Download/" />

    <!-- 或者直接放开整个外部应用专属目录（更宽松）：
    <external-files-path name="external_files_all" path="." />
    -->
</paths>
